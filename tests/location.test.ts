import { describe, it } from 'vitest';

describe('Location Services (Phase 1)', () => {
  it.todo('LOC-01: Request browser/device GPS on page load with graceful fallback');
  it.todo('LOC-02: Allow manual city/area search if GPS is denied');
  it.todo('LOC-03: Calculate and display distance (km) from user to each listing');
  it.todo('LOC-04: Sort listings by distance ascending (nearest first) by default');
  it.todo('LOC-05: Display listings on an interactive map (Mapbox GL JS)');
  it.todo('LOC-06: Update results dynamically on map pan/zoom');
});
