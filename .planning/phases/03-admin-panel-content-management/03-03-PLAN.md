---
phase: 03-admin-panel-content-management
plan: 03
type: execute
wave: 2
depends_on: ["03-00", "03-01", "03-02"]
files_modified: ["src/app/admin/page.tsx", "src/components/admin/AdminTabs.tsx", "src/components/admin/ListingApprovalCard.tsx", "src/components/admin/StatsCard.tsx", "src/components/admin/AdminQueueCard.tsx", "src/components/admin/AdminReviewPanel.tsx", "src/components/admin/AdminRejectionModal.tsx", "src/components/admin/PublicParkingTable.tsx", "src/components/admin/OwnerSubscriptionTable.tsx", "src/components/admin/ExportToolbar.tsx"]
autonomous: true
requirements: ["ADM-01", "ADM-02", "ADM-03", "ADM-04", "ADM-05", "ADM-06", "ADM-07", "ADM-08"]
user_setup: []

must_haves:
  truths:
    - "The admin landing experience is an action center with KPIs and priority queues on the same screen"
    - "Admins can review full listing submissions before acting"
    - "Payment verification, public parking management, owner/subscription oversight, and export flows are available from the admin UI"
    - "Queue progression and structured rejection patterns match the approved UI contract"
  artifacts:
    - path: "src/app/admin/page.tsx"
      provides: "Admin action-center composition root"
    - path: "src/components/admin/AdminReviewPanel.tsx"
      provides: "Full-detail listing or payment review surface"
    - path: "src/components/admin/AdminRejectionModal.tsx"
      provides: "Shared rejection modal with required category"
    - path: "src/components/admin/PublicParkingTable.tsx"
      provides: "Public parking management UI"
    - path: "src/components/admin/OwnerSubscriptionTable.tsx"
      provides: "Owner and subscription management UI"
    - path: "src/components/admin/ExportToolbar.tsx"
      provides: "Filtered export controls"
  key_links:
    - from: "src/app/admin/page.tsx"
      to: "src/app/api/admin/*"
      via: "fetch-driven dashboard sections"
      pattern: "api/admin"
    - from: "src/components/admin/AdminRejectionModal.tsx"
      to: "listing and payment mutation routes"
      via: "required rejection payload"
      pattern: "category|note"

---

<objective>
Refactor and complete the admin UI around the new Phase 3 backend contracts.

Purpose: Deliver the admin action center and management surfaces that let admins review listings, verify payments, manage public parking, oversee owners/subscriptions, and export filtered datasets.

Output: A responsive admin dashboard that matches `03-UI-SPEC.md` and the richer admin APIs built in earlier plans.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-admin-panel-content-management/03-CONTEXT.md
@.planning/phases/03-admin-panel-content-management/03-RESEARCH.md
@.planning/phases/03-admin-panel-content-management/03-VALIDATION.md
@.planning/phases/03-admin-panel-content-management/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor the admin page into an action-center composition root</name>
  <files>src/app/admin/page.tsx, src/components/admin/AdminTabs.tsx, src/components/admin/StatsCard.tsx, src/components/admin/AdminQueueCard.tsx</files>
  <action>Rework the admin page so the default view matches `D-01` and the UI spec: KPI summary plus the highest-priority listing and payment queues above the fold. Keep tabs or segmented navigation for `Overview`, `Listing Review`, `Payment Verification`, `Public Parking`, `Owners & Subscriptions`, and `Exports`. Extract reusable queue and KPI components instead of continuing to grow a single page component.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin-dashboard.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Admin landing page behaves as an action center with extracted overview components</done>
</task>

<task type="auto">
  <name>Task 2: Implement full-detail review and structured rejection UX</name>
  <files>src/components/admin/ListingApprovalCard.tsx, src/components/admin/AdminReviewPanel.tsx, src/components/admin/AdminRejectionModal.tsx</files>
  <action>Upgrade listing and payment review interactions so admins can inspect the full submission before acting, use a shared rejection modal with required category plus optional note, see the exact owner-facing rejection preview, and receive inline success feedback followed by automatic progression to the next item in the active queue.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin-dashboard.test.tsx</automated>
  </verify>
  <done>Review surfaces support full detail, mandatory rejection reasons, and next-item progression</done>
</task>

<task type="auto">
  <name>Task 3: Build public parking, owner/subscription, and export management surfaces</name>
  <files>src/components/admin/PublicParkingTable.tsx, src/components/admin/OwnerSubscriptionTable.tsx, src/components/admin/ExportToolbar.tsx, src/app/admin/page.tsx</files>
  <action>Implement the table-first public parking management view with archive-only row actions and create/edit entry points, the owner/subscription management table with status badges and quick actions, and the export toolbar that scopes CSV downloads to the current filtered dataset. Keep filter state stable while switching sections and align copy, spacing, status colors, and keyboard accessibility with `03-UI-SPEC.md`.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin-dashboard.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>All Phase 3 admin management surfaces are reachable from the UI and use the approved interaction model</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI -> Admin API | Privileged browser actions |
| Admin UI -> Export/download flow | Dataset export initiated from the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Elevation of Privilege | UI-triggered admin actions | mitigate | Route every privileged action through server-validated admin endpoints; never trust client state alone |
| T-03-09 | Repudiation | Rejection and lifecycle controls | mitigate | Require explicit confirmation and structured reason capture before destructive or blocking actions |
| T-03-10 | Information Disclosure | Full-detail review panels | mitigate | Limit rendered data to admin-only views and avoid leaking raw secrets or tokens into the page |
</threat_model>

<verification>
Verify the admin page renders the action center, review panels expose full detail, rejection modals enforce required categories, quick actions stay keyboard reachable, and export/public parking/owner-subscription sections honor the approved UI contract.
</verification>

<success_criteria>
- Admin overview combines KPIs and priority queues above the fold
- Listing and payment review flows show full detail before action
- Rejection UX is shared and requires structured reasons
- Public parking, owner/subscription, and export sections are functional entry points
- Component tests cover the main action-center and management behaviors
</success_criteria>

<output>
After completion, create .planning/phases/03-admin-panel-content-management/03-03-SUMMARY.md
</output>
