---
phase: 03-admin-panel-content-management
plan: 01
type: execute
wave: 1
depends_on: ["03-00"]
files_modified: ["prisma/schema.prisma", "src/lib/admin-auth.ts", "src/app/api/admin/listings/route.ts", "src/app/api/admin/listings/[id]/route.ts", "src/app/api/admin/subscriptions/pending/route.ts", "src/app/api/admin/subscriptions/verify/route.ts"]
autonomous: true
requirements: ["ADM-01", "ADM-02", "ADM-05", "ADM-06"]
user_setup: []

must_haves:
  truths:
    - "Admin listing and payment review use stable server-side contracts"
    - "Rejection flows capture structured reasons"
    - "Subscription lifecycle changes and listing visibility stay coupled"
    - "Admin auth checks are centralized instead of duplicated in every route"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Admin-ready listing, subscription, and audit schema"
    - path: "src/lib/admin-auth.ts"
      provides: "Shared admin auth helper"
      exports: ["requireAdminSession"]
    - path: "src/app/api/admin/listings/[id]/route.ts"
      provides: "Listing review mutation contract"
      exports: ["PATCH"]
    - path: "src/app/api/admin/subscriptions/verify/route.ts"
      provides: "Payment verification and manual subscription action contract"
      exports: ["POST"]
  key_links:
    - from: "src/app/api/admin/subscriptions/verify/route.ts"
      to: "prisma/schema.prisma"
      via: "transactional subscription and listing state changes"
      pattern: "transaction|update"
    - from: "src/app/api/admin/listings/[id]/route.ts"
      to: "owner correction loop"
      via: "stored rejection category and note"
      pattern: "REJECT"

---

<objective>
Strengthen the admin domain model and core review APIs for listings and payments.

Purpose: Lock down the server-side rules that the admin UI will depend on for review, rejection, verification, and manual subscription control.

Output: Expanded schema plus shared admin helpers and upgraded listing/payment routes.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-admin-panel-content-management/03-CONTEXT.md
@.planning/phases/03-admin-panel-content-management/03-RESEARCH.md
@.planning/phases/03-admin-panel-content-management/03-VALIDATION.md
@.planning/phases/03-admin-panel-content-management/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand schema for admin review, lifecycle control, and audit history</name>
  <files>prisma/schema.prisma</files>
  <action>Update `prisma/schema.prisma` to support Phase 3 admin requirements. Add the missing metadata needed for full-detail review and public/admin management, including structured rejection fields, archive markers, richer listing attributes, subscription lifecycle metadata, and a durable admin audit log model. Preserve existing owner-flow compatibility while making the model expressive enough for admin review, manual renew/extend actions, and recent activity reporting.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Schema validates with admin-ready review and audit fields</done>
</task>

<task type="auto">
  <name>Task 2: Centralize admin auth and filter parsing</name>
  <files>src/lib/admin-auth.ts</files>
  <action>Create `src/lib/admin-auth.ts` with a shared `requireAdminSession` helper that encapsulates bearer-token parsing, Supabase user lookup, whitelist enforcement, and dev bypass handling. Include any small shared helpers needed for common admin query filters so later routes stop duplicating request parsing logic.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin-routes.test.ts</automated>
  </verify>
  <done>Admin routes can use one shared auth helper consistently</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade listing review endpoints for full-detail review and structured rejection</name>
  <files>src/app/api/admin/listings/route.ts, src/app/api/admin/listings/[id]/route.ts</files>
  <action>Refactor the admin listing routes to use the shared admin helper and richer schema. Support queue filters for status/date/search, return the full submission detail needed for review, require rejection category plus optional note on reject, preserve owner-visible correction guidance, and ensure rejected listings remain editable and resubmittable as the same record. Return payloads shaped for the action-center and review panel UX in `03-UI-SPEC.md`.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin-routes.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Listing review APIs support full detail, filters, and structured rejection</done>
</task>

<task type="auto">
  <name>Task 4: Upgrade subscription verification endpoints for manual lifecycle control</name>
  <files>src/app/api/admin/subscriptions/pending/route.ts, src/app/api/admin/subscriptions/verify/route.ts</files>
  <action>Expand subscription admin routes to cover payment approval and rejection, manual activate/deactivate, renew, extend, grace-period state, and audit history. Keep the listing visibility coupling inside the mutation layer so active payment state and listing status cannot drift. Preserve a queue endpoint for pending payment review and return the summary data needed for quick progression to the next item.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin-routes.test.ts && npx prisma validate</automated>
  </verify>
  <done>Subscription admin APIs expose manual lifecycle control with audit-safe mutations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin client -> Admin API | Privileged mutations from the admin UI |
| Admin API -> Database | Transactional writes to listings, subscriptions, and audit history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Elevation of Privilege | Admin routes | mitigate | Centralize admin auth checks and keep dev bypass isolated behind explicit env checks |
| T-03-03 | Tampering | Listing/subscription state coupling | mitigate | Use transactional mutations and test both sides of every lifecycle change |
| T-03-04 | Repudiation | Manual verification and rejection decisions | mitigate | Persist audit log entries with actor, timestamps, and structured reasons |
</threat_model>

<verification>
Verify listing and subscription admin mutations reject unauthorized callers, require structured rejection reasons where applicable, preserve audit history, and keep listing visibility synchronized with subscription status.
</verification>

<success_criteria>
- Schema supports admin rejection, archival, and audit metadata
- Shared admin auth helper is used by upgraded routes
- Listing review APIs expose full-detail review and structured rejection flows
- Subscription verification APIs support manual lifecycle control
- Route tests cover the main privileged mutation paths
</success_criteria>

<output>
After completion, create .planning/phases/03-admin-panel-content-management/03-01-SUMMARY.md
</output>
