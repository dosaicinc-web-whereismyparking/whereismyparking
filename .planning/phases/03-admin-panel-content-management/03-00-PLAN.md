---
phase: 03-admin-panel-content-management
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: ["tests/admin-routes.test.ts", "tests/admin-dashboard.test.tsx", "tests/setup.ts"]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Phase 3 has dedicated route and component verification coverage"
    - "Admin-only route flows can be tested with stable fixtures"
    - "Later plans can rely on quick feedback loops for admin regressions"
  artifacts:
    - path: "tests/admin-routes.test.ts"
      provides: "Admin route behavior coverage"
    - path: "tests/admin-dashboard.test.tsx"
      provides: "Admin UI behavior coverage"
    - path: "tests/setup.ts"
      provides: "Shared admin test setup"
  key_links:
    - from: "tests/admin-routes.test.ts"
      to: "src/app/api/admin/*"
      via: "imported route handlers"
      pattern: "api/admin"
    - from: "tests/admin-dashboard.test.tsx"
      to: "src/app/admin/page.tsx"
      via: "component render assertions"
      pattern: "AdminDashboard"

---

<objective>
Initialize Phase 3 validation scaffolding for admin routes and admin UI flows.

Purpose: Give the rest of the phase fast regression checks before schema, API, and page work starts.

Output: Admin-specific Vitest coverage stubs or tests that exercise the main route and page contracts.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-admin-panel-content-management/03-RESEARCH.md
@.planning/phases/03-admin-panel-content-management/03-VALIDATION.md
@.planning/phases/03-admin-panel-content-management/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add admin route test coverage</name>
  <files>tests/admin-routes.test.ts</files>
  <action>Create `tests/admin-routes.test.ts` to cover the current and planned admin route contracts. Include admin-auth-bypass fixtures, listing approval/rejection expectations, payment verification expectations, subscription state-to-listing visibility coupling, public parking archive contract, and CSV export response shape. Use mocks for Prisma and Supabase so the file can evolve alongside the routes without requiring live services.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin-routes.test.ts</automated>
  </verify>
  <done>Admin route test file exists and runs under Vitest</done>
</task>

<task type="auto">
  <name>Task 2: Add admin dashboard component coverage</name>
  <files>tests/admin-dashboard.test.tsx, tests/setup.ts</files>
  <action>Create `tests/admin-dashboard.test.tsx` for the admin page and supporting components. Cover the action-center overview render, tab/section changes, queue empty states, required rejection-flow affordances, and export control visibility. Update `tests/setup.ts` only as needed for fetch mocks, `matchMedia`, or router shims used by the admin dashboard tests.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin-dashboard.test.tsx</automated>
  </verify>
  <done>Admin dashboard tests run with shared setup in place</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tests -> Admin routes | Mocked route execution for privileged flows |
| Tests -> Admin UI | Simulated admin-only client interactions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-00 | Tampering | Test fixtures | mitigate | Centralize auth-bypass fixtures and shared mocks in one place |
| T-03-01 | Repudiation | Validation coverage | mitigate | Add explicit assertions for route outcomes and required fields rather than smoke-only tests |
</threat_model>

<verification>
Verify both new admin test files execute cleanly and fail meaningfully if route contracts or admin UI expectations regress.
</verification>

<success_criteria>
- `tests/admin-routes.test.ts` exists and runs
- `tests/admin-dashboard.test.tsx` exists and runs
- `tests/setup.ts` supports the new admin tests without breaking earlier phases
- Phase 3 quick validation command from `03-VALIDATION.md` becomes runnable
</success_criteria>

<output>
After completion, create .planning/phases/03-admin-panel-content-management/03-00-SUMMARY.md
</output>
