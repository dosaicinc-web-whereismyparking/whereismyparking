---
phase: 03-admin-panel-content-management
plan: 02
type: execute
wave: 1
depends_on: ["03-00", "03-01"]
files_modified: ["src/app/api/admin/stats/route.ts", "src/app/api/admin/owners/route.ts", "src/app/api/admin/public-parking/route.ts", "src/app/api/admin/public-parking/[id]/route.ts", "src/app/api/admin/export/route.ts", "scratch/admin-public-import-api.md"]
autonomous: true
requirements: ["ADM-03", "ADM-04", "ADM-05", "ADM-07", "ADM-08"]
user_setup: []

must_haves:
  truths:
    - "Admins can manage public parking records directly"
    - "Owner and subscription oversight exposes lifecycle state and linked listing context"
    - "KPI and export data are filter-aware and generated server-side"
    - "Bulk import is documented against the same public parking model used by CRUD"
  artifacts:
    - path: "src/app/api/admin/public-parking/route.ts"
      provides: "Public parking list and create contract"
      exports: ["GET", "POST"]
    - path: "src/app/api/admin/public-parking/[id]/route.ts"
      provides: "Public parking update and archive contract"
      exports: ["PATCH"]
    - path: "src/app/api/admin/export/route.ts"
      provides: "Server-side CSV export contract"
      exports: ["GET"]
    - path: "scratch/admin-public-import-api.md"
      provides: "Bulk import API contract and field mapping notes"
  key_links:
    - from: "src/app/api/admin/export/route.ts"
      to: "stats/owners/public parking routes"
      via: "shared filter semantics"
      pattern: "status|dateRange|dataset"
    - from: "scratch/admin-public-import-api.md"
      to: "src/app/api/admin/public-parking/route.ts"
      via: "same record shape and validation rules"
      pattern: "public parking"

---

<objective>
Build the reporting, public parking management, and export layer for the Phase 3 admin system.

Purpose: Provide the server-side surfaces that power KPI filtering, owner oversight, public parking CRUD, CSV export, and future bulk ingestion.

Output: New or expanded admin APIs for public parking, reporting, and export, plus a documented bulk import contract.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-admin-panel-content-management/03-CONTEXT.md
@.planning/phases/03-admin-panel-content-management/03-RESEARCH.md
@.planning/phases/03-admin-panel-content-management/03-VALIDATION.md
@.planning/phases/03-admin-panel-content-management/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand KPI and owner/subscription reporting endpoints</name>
  <files>src/app/api/admin/stats/route.ts, src/app/api/admin/owners/route.ts</files>
  <action>Refactor `stats` and `owners` routes to use the shared admin auth helper and upgraded schema. Add the KPI coverage required by `D-17` and support date-range and status filters where relevant. Ensure owner/subscription responses expose expiry, grace-period state, linked listing counts, and the data needed for inline lifecycle actions in the UI.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin-routes.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Stats and owner reporting endpoints support richer metrics and filters</done>
</task>

<task type="auto">
  <name>Task 2: Add public parking CRUD and archival APIs</name>
  <files>src/app/api/admin/public-parking/route.ts, src/app/api/admin/public-parking/[id]/route.ts</files>
  <action>Create admin-only public parking routes for list/create/update/archive flows. Use a table-first contract: filters, row-level actions, and archived visibility. Enforce archive-only deletion semantics, validate the richer public record fields from `D-12`, and shape the API so the same model can later accept bulk import input without a parallel ingestion schema.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin-routes.test.ts && npx prisma validate</automated>
  </verify>
  <done>Public parking CRUD and archive behavior exist behind admin-only APIs</done>
</task>

<task type="auto">
  <name>Task 3: Add server-side CSV export and bulk import contract documentation</name>
  <files>src/app/api/admin/export/route.ts, scratch/admin-public-import-api.md</files>
  <action>Create a server-side CSV export route that supports dataset selection, status filtering, and date-range filtering for listings, owners, subscriptions, and payment verification history. In parallel, write `scratch/admin-public-import-api.md` describing the future bulk import endpoint contract, required fields, validation rules, duplicate handling, and how imported records map onto the same public parking CRUD model introduced in this plan.</action>
  <verify>
    <automated>npm run test:unit -- tests/admin-routes.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Filtered CSV export and public parking bulk import contract are documented and test-covered</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin client -> Reporting/export APIs | Privileged data access with broad record visibility |
| Admin API -> CSV consumers | Serialized datasets leaving the web UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | Export endpoints | mitigate | Keep export behind admin auth, require explicit dataset selection, and respect filters intentionally |
| T-03-06 | Tampering | Public parking archive/delete behavior | mitigate | Implement archive-only mutations and test restore/archive transitions |
| T-03-07 | Denial of Service | Large filtered exports | mitigate | Bound export filters and dataset generation paths so the route does not stream arbitrary unbounded queries |
</threat_model>

<verification>
Verify KPI endpoints return filtered aggregates, owner/subscription data exposes lifecycle context, public parking routes support archive-safe CRUD, and export responses produce the expected CSV headers and filtered row sets.
</verification>

<success_criteria>
- KPI endpoint covers the Phase 3 metric set with filters
- Owner/subscription route returns lifecycle and linked-listing context
- Public parking CRUD/archive routes exist and validate rich records
- CSV export works server-side for the required datasets
- Bulk import contract is documented against the same data model
</success_criteria>

<output>
After completion, create .planning/phases/03-admin-panel-content-management/03-02-SUMMARY.md
</output>
