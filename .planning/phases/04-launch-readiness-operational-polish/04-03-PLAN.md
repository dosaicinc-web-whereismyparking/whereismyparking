---
phase: 04-launch-readiness-operational-polish
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified: ["src/components/ParkingCard.tsx", "src/components/Image.tsx", "next.config.js", "src/app/layout.tsx"]
autonomous: true
requirements: ["PERF-01"]
user_setup: []

must_haves:
  truths:
    - "4G initial page load verified at <2.0s via Lighthouse audit"
  artifacts:
    - path: "src/components/Image.tsx"
      provides: "Optimized image wrapper"
      exports: ["OptimizedImage"]
    - path: "next.config.js"
      provides: "Performance config"
      contains: "images"
  key_links:
    - from: "src/components/ParkingCard.tsx"
      to: "src/components/Image.tsx"
      via: "import OptimizedImage"
      pattern: "OptimizedImage"
    - from: "next.config.js"
      to: "build output"
      via: "image optimization"
      pattern: "images"
---

<objective>
Optimize performance for <2s load on 4G mobile networks

Purpose: Ensure fast loading for target users in India
Output: Optimized images, lazy loading, and passing Lighthouse audit
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-launch-readiness-operational-polish/04-RESEARCH.md
@src/components/ParkingCard.tsx
@next.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OptimizedImage component</name>
  <files>src/components/Image.tsx</files>
  <action>Create wrapper around Next.js Image component with default props: priority for above-fold, lazy for below, alt text required, sizes for responsive. Handle Supabase storage URLs.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Component optimizes images automatically</done>
</task>

<task type="auto">
  <name>Task 2: Update ParkingCard to use OptimizedImage</name>
  <files>src/components/ParkingCard.tsx</files>
  <action>Replace img tags with OptimizedImage component. Set priority for first card, lazy for others. Add alt text with parking name.</action>
  <verify>
    <automated>grep -n "OptimizedImage" src/components/ParkingCard.tsx</automated>
  </verify>
  <done>ParkingCard uses optimized images</done>
</task>

<task type="auto">
  <name>Task 3: Configure Next.js for performance</name>
  <files>next.config.js</files>
  <action>Add images config: domains for Supabase storage, formats: ['image/webp', 'image/avif'], deviceSizes and imageSizes for mobile optimization. Enable experimental features if needed.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Next.js config optimized for images</done>
</task>

<task type="auto">
  <name>Task 4: Run Lighthouse audit and verify</name>
  <files>src/app/layout.tsx</files>
  <action>Install lighthouse if needed, run audit on production build. Target: Performance >85, FCP <1.8s, LCP <2.5s. Fix any critical issues found.</action>
  <verify>
    <automated>lighthouse http://localhost:3000 --output=json --output-path=./lighthouse-report.json && cat lighthouse-report.json | jq '.categories.performance.score'</automated>
  </verify>
  <done>Lighthouse performance score >85</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→CDN | Image loading from Supabase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Denial of Service | Image loading | accept | External CDN, low risk |
</threat_model>

<verification>
Lighthouse audit passes with score >85, manual 4G simulation shows <2s load.
</verification>

<success_criteria>
Performance optimized, Lighthouse audit confirms <2s load on 4G.
</success_criteria>

<output>
After completion, create .planning/phases/04-launch-readiness-operational-polish/04-03-SUMMARY.md
</output>