---
phase: 04-launch-readiness-operational-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: ["src/components/NavigateButton.tsx", "src/components/ParkingCard.tsx", "src/app/layout.tsx", "src/app/page.tsx", "src/app/listings/[id]/page.tsx"]
autonomous: true
requirements: ["SEO-01", "SEO-02", "SEO-03", "SEO-04", "SEO-05", "NAV-01"]
user_setup: []

must_haves:
  truths:
    - "All pages have optimized meta-tags and SEO titles"
    - "Navigation deep-links work consistently across all listing types"
  artifacts:
    - path: "src/components/NavigateButton.tsx"
      provides: "Unified navigation component"
      exports: ["NavigateButton"]
    - path: "src/app/layout.tsx"
      provides: "Global metadata"
      contains: "export const metadata"
    - path: "src/app/listings/[id]/page.tsx"
      provides: "Dynamic metadata for listings"
      contains: "generateMetadata"
  key_links:
    - from: "src/components/ParkingCard.tsx"
      to: "src/components/NavigateButton.tsx"
      via: "import and render"
      pattern: "NavigateButton"
    - from: "src/app/layout.tsx"
      to: "page source"
      via: "Next.js metadata API"
      pattern: "<meta"
---

<objective>
Implement SEO optimization and consistent navigation deep-links

Purpose: Improve search engine visibility and ensure reliable navigation to parking locations
Output: Pages with proper metadata and unified navigation component
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-launch-readiness-operational-polish/04-RESEARCH.md
@src/app/layout.tsx
@src/components/ParkingCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unified NavigateButton component</name>
  <files>src/components/NavigateButton.tsx</files>
  <action>Create React component that takes latitude, longitude, name as props. Generate Google Maps deep-link URL: `https://www.google.com/maps/dir/?api=1&destination=${lat},${lng}&destination_place_id=${name}`. Handle click to open in new tab or app. Include accessibility attributes. Use Tailwind classes for styling.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Component renders button that opens maps on click</done>
</task>

<task type="auto">
  <name>Task 2: Update ParkingCard to use NavigateButton</name>
  <files>src/components/ParkingCard.tsx</files>
  <action>Replace existing navigation logic with NavigateButton component. Pass lat, lng, name props. Ensure consistent styling and placement.</action>
  <verify>
    <automated>grep -n "NavigateButton" src/components/ParkingCard.tsx</automated>
  </verify>
  <done>ParkingCard uses NavigateButton for navigation</done>
</task>

<task type="auto">
  <name>Task 3: Add global SEO metadata to layout</name>
  <files>src/app/layout.tsx</files>
  <action>Add export const metadata with title: "WhereIsMyParking - Find Parking in India", description: "Discover nearby parking spaces in urban India. Public and private parking available.", keywords: ["parking", "India", "urban", "GPS"], openGraph: {title, description, type: "website"}, twitter: {card: "summary"}. Include canonical URL.</action>
  <verify>
    <automated>curl -s http://localhost:3000 | grep "WhereIsMyParking"</automated>
  </verify>
  <done>Layout includes comprehensive metadata</done>
</task>

<task type="auto">
  <name>Task 4: Add metadata to home page</name>
  <files>src/app/page.tsx</files>
  <action>Add export const metadata with specific title and description for home page. Include structured data (JSON-LD) for organization and website.</action>
  <verify>
    <automated>curl -s http://localhost:3000 | grep '<script type="application/ld+json"'</automated>
  </verify>
  <done>Home page has enhanced metadata and structured data</done>
</task>

<task type="auto">
  <name>Task 5: Add dynamic metadata to listing pages</name>
  <files>src/app/listings/[id]/page.tsx</files>
  <action>Add generateMetadata function that fetches listing data and returns metadata with title: "{name} Parking - WhereIsMyParking", description: "Parking at {address}, {type}", openGraph image if available.</action>
  <verify>
    <automated>curl -s http://localhost:3000/listings/1 | grep "Parking - WhereIsMyParking"</automated>
  </verify>
  <done>Listing pages have dynamic SEO metadata</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→External | Deep-links to Google Maps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | Metadata | accept | Public SEO data, no sensitive info |
</threat_model>

<verification>
Check page sources for meta tags, test navigation buttons open correct maps URLs.
</verification>

<success_criteria>
All pages display proper meta tags in source, navigation deep-links open Google Maps with correct destinations.
</success_criteria>

<output>
After completion, create .planning/phases/04-launch-readiness-operational-polish/04-02-SUMMARY.md
</output>