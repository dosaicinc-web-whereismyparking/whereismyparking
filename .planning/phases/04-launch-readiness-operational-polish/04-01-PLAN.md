---
phase: 04-launch-readiness-operational-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/components/admin/PublicParkingForm.tsx", "src/app/admin/public-parking/page.tsx", "src/app/api/admin/public-parking/route.ts"]
autonomous: true
requirements: ["ADM-03"]
user_setup: []

must_haves:
  truths:
    - "Admin can add new public parking records via form"
  artifacts:
    - path: "src/components/admin/PublicParkingForm.tsx"
      provides: "Form component for public parking data"
      min_lines: 50
    - path: "src/app/admin/public-parking/page.tsx"
      provides: "Admin page with form"
      contains: "PublicParkingForm"
    - path: "src/app/api/admin/public-parking/route.ts"
      provides: "API endpoint for creating public parking"
      exports: ["POST"]
  key_links:
    - from: "src/components/admin/PublicParkingForm.tsx"
      to: "/api/admin/public-parking"
      via: "fetch POST on submit"
      pattern: "fetch.*api/admin/public-parking"
    - from: "src/app/api/admin/public-parking/route.ts"
      to: "public_parking table"
      via: "supabase insert"
      pattern: "supabase.*insert"
---

<objective>
Enhance admin panel with form for adding public parking records

Purpose: Enable admin to directly add public parking data without owner submissions
Output: Functional form in admin panel that saves to database
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-launch-readiness-operational-polish/04-RESEARCH.md
@src/app/admin/layout.tsx
@src/components/admin/AdminLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PublicParkingForm component</name>
  <files>src/components/admin/PublicParkingForm.tsx</files>
  <action>Create React component using React Hook Form and Zod for validation. Include fields: name (string, 3-100 chars), address (string, 10-500 chars), latitude (number, -90 to 90), longitude (number, -180 to 180), type (select: Public), coverage (select: Open-air/Covered/Multi-level), operating_hours (string), vehicle_types (multi-select: Car/Bike/Truck). Use shadcn/ui Input, Select, Textarea components. On submit, POST to /api/admin/public-parking with form data. Handle success/error states with toast notifications.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Component renders form with all required fields and validation</done>
</task>

<task type="auto">
  <name>Task 2: Add admin public-parking page</name>
  <files>src/app/admin/public-parking/page.tsx</files>
  <action>Create Next.js page that imports and renders PublicParkingForm component. Add page title "Add Public Parking". Ensure page is protected by admin authentication (check session).</action>
  <verify>
    <automated>curl -s http://localhost:3000/admin/public-parking | grep "Add Public Parking"</automated>
  </verify>
  <done>Page loads and displays the form</done>
</task>

<task type="auto">
  <name>Task 3: Create API endpoint for public parking creation</name>
  <files>src/app/api/admin/public-parking/route.ts</files>
  <action>Create POST handler that validates admin session, parses request body with Zod schema matching form fields, inserts record into public_parking table via Supabase client. Return 201 with created record on success, 400/401/500 with error messages on failure.</action>
  <verify>
    <automated>npm test -- --testPathPattern=admin/public-parking</automated>
  </verify>
  <done>API accepts POST requests and saves data to database</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Admin form submits data to protected endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Injection | PublicParkingForm | mitigate | Validate input with Zod at API level |
| T-04-02 | Spoofing | Admin session | mitigate | Verify JWT token before allowing POST |
</threat_model>

<verification>
Test end-to-end: Admin logs in, navigates to /admin/public-parking, fills form, submits, verifies record appears in database.
</verification>

<success_criteria>
Admin can successfully add new public parking records through the form, data persists in database.
</success_criteria>

<output>
After completion, create .planning/phases/04-launch-readiness-operational-polish/04-01-SUMMARY.md
</output>