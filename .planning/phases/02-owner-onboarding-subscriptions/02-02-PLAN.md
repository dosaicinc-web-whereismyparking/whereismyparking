# Plan: Phase 02 - Plan 02 - Owner Dashboard & Multi-step Form UI

**Wave:** 2
**Depends on:** 02-01-PLAN.md
**Files Modified:** `src/app/dashboard/page.tsx` (new), `src/components/ListingForm.tsx` (new), `src/components/UpiPayment.tsx` (new)
**Autonomous:** true

## Objective
Implement the user interface for owners to manage their listings and complete the onboarding flow with UPI payment integration.

## Tasks

<task>
<read_first>
- `src/app/page.tsx` (for layout consistency)
- `src/lib/supabase-types.ts`
</read_first>
<action>
Create `src/app/dashboard/page.tsx`:
1. Implement a dashboard view for authenticated owners.
2. Fetch listings via `GET /api/listings/owner` (to be added in Wave 1).
3. Display cards with status tags (Pending, Active, Expired, Rejected).
4. Add "Add New Parking" FAB (Floating Action Button).
</action>
<acceptance_criteria>
- Dashboard rendered correctly at `/dashboard`.
- Listing status badges show correct colors (Green for Active, Amber for Pending, etc.).
- "Add New" button triggers form.
</acceptance_criteria>
</task>

<task>
<read_first>
- `src/components/Map.tsx`
- `package.json` (check react-hook-form)
</read_first>
<action>
Create `src/components/ListingForm.tsx`:
1. Use `react-hook-form` to manage multi-step state.
2. Step 1: Text inputs for Name, Address, City.
3. Step 2: Integrated Mapbox Map with a draggable marker to select GPS location.
4. Step 3: Selection for ParkingType, Coverage, and Vehicle specific checkboxes.
</action>
<acceptance_criteria>
- Form steps navigate correctly (Back/Next).
- Map pin correctly updates Lat/Lng state.
- Zod validation prevents submission without location.
</acceptance_criteria>
</task>

<task>
<read_first>
- `src/components/ListingForm.tsx`
</read_first>
<action>
Create `src/components/UpiPayment.tsx`:
1. Generate UPI intent URL using listing ID.
2. Step 4 of the form: Show "Pay ₹499" button (opens deep link).
3. Provide input field for UTR submission.
4. Final "Submit Listing" button.
</action>
<acceptance_criteria>
- UPI link contains correct `pa` and `am=499` parameters.
- UTR submission calls `/api/subscriptions/submit-utr`.
</acceptance_criteria>
</task>

## Verification
- Test UPI deep-link on a mobile browser (simulated or manual).
- Ensure listing creation flow results in a successful DB entry and a redirect back to dashboard.
