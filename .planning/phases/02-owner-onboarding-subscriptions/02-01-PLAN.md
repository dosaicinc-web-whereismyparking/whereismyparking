# Plan: Phase 02 - Plan 01 - Database & Backend Core

**Wave:** 1
**Depends on:** Phase 01 Complete
**Files Modified:** `prisma/schema.prisma`, `src/lib/supabase-types.ts`, `src/app/api/parking/route.ts` (new), `src/app/api/subscriptions/route.ts` (new)
**Autonomous:** true

## Objective
Establish the data models and backend API endpoints required for owner listing management and manual subscription verification.

## Tasks

<task>
<read_first>
- `prisma/schema.prisma`
- `supabase/migrations/01_initial.sql`
</read_first>
<action>
Update `prisma/schema.prisma`:
1. Add `Subscription` model with fields for current status, UTR, and dates.
2. Update `ParkingListing` model to include `ownerId` (String).
3. Add `SubscriptionStatus` enum.
4. Add `owner` relation to `User` in `ParkingListing`.
</action>
<acceptance_criteria>
- `npx prisma validate` passes.
- `Subscription` model present in schema.
- `ParkingListing` has `ownerId` field.
</acceptance_criteria>
</task>

<task>
<read_first>
- `prisma/schema.prisma`
</read_first>
<action>
Execute migration:
1. `npx prisma migrate dev --name add_subscriptions_and_owner_relation`
2. This will generate the SQL and update the database (local/Supabase).
</action>
<acceptance_criteria>
- Migration directory created in `prisma/migrations`.
- Database schema updated.
- `npx prisma generate` successful.
</acceptance_criteria>
</task>

<task>
<read_first>
- `src/lib/prisma.ts`
</read_first>
<action>
Create API route `src/app/api/listings/route.ts`:
1. POST: Create listing using `$executeRaw` for PostGIS point insertion.
2. Ensure `ownerId` is extracted from auth context (JWT).
3. Implement Zod validation for listing payload.
</action>
<acceptance_criteria>
- API endpoint `POST /api/listings` exists.
- Correctly inserts `geometry(Point, 4326)` into database.
- Rejects requests without valid owner JWT.
</acceptance_criteria>
</task>

<task>
<read_first>
- `src/lib/prisma.ts`
</read_first>
<action>
Create API route `src/app/api/subscriptions/initiate/route.ts`:
1. POST: Creates a new Subscription record for a listing.
2. Return the listing ID and status.
</action>
<acceptance_criteria>
- API returns 201 on successful record creation.
</acceptance_criteria>
</task>

<task>
<read_first>
- `src/lib/prisma.ts`
</read_first>
<action>
Create API route `src/app/api/subscriptions/submit-utr/route.ts`:
1. POST: Updates Subscription record with UTR and moves status to `PENDING_VERIFICATION`.
</action>
<acceptance_criteria>
- UTR updated in DB.
- Status changed to `PENDING_VERIFICATION`.
</acceptance_criteria>
</task>

## Verification
- Run `vitest` unit tests for UPI link generation logic (to be added in next plan/utils).
- Manual API testing via Postman/Curl for listing creation.
