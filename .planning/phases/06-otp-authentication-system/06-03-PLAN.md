---
wave: 3
depends_on:
  - 06-02
files_modified:
  - src/app/api/auth/send-otp/route.ts
autonomous: true
---

# Plan 06-03: Resend OTP with Cooldown

Implement the 60-second cooldown logic to prevent SMS spam and provide user feedback on remaining wait time.

## Success Criteria

- [ ] `POST /api/auth/send-otp` rejects request if last OTP was sent < 60s ago
- [ ] Response includes `seconds_remaining` in the error/cooldown body

## Tasks

<task identifier="06-03-01" autonomous="true">
<read_first>
- c:\Users\ACER\Desktop\SOUP\src\app\api\auth\send-otp/route.ts
</read_first>
<action>
Update `src/app/api/auth/send-otp/route.ts` to check `otp_sessions.last_sent_at` (need to add this column or use `created_at` if we recreate session on every send):
1. Query `otp_sessions` for the `phone`.
2. Calculate `elapsed = now() - last_sent_at`.
3. If `elapsed < 60 seconds`:
   - Calculate `remaining = 60 - elapsed`.
   - Return 429 Error with `{ error: "Cooldown active", seconds_remaining: remaining }`.
</action>
<acceptance_criteria>
- Consecutive calls to `send-otp` within 60s return 429.
- Response contains `seconds_remaining` key.
</acceptance_criteria>
</task>

## Verification

### Automated Tests
- Call `send-otp` twice in 5 seconds. Verify second call fails with `seconds_remaining` around 55.

### Manual Verification
- Attempt to resend OTP immediately from UI (if integrated) or Postman and verify the block.
