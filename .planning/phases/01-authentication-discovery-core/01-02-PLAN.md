---
phase: 01-authentication-discovery-core
plan: 02
type: execute
wave: 1
depends_on: ["01-00"]
files_modified: ["docker-compose.yml", "supabase/migrations/01_initial.sql", ".env.local"]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Parking listings stored with location geometry"
    - "Users table supports OTP authentication"
    - "Admin whitelist table exists"
    - "PostGIS enabled for geospatial queries"
    - "Supabase running locally via Docker Compose"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Self-hosted Supabase local environment"
      contains: "supabase/postgres, supabase/auth, supabase/realtime"
    - path: "supabase/migrations/01_initial.sql"
      provides: "Database migration script"
      contains: "CREATE TABLE parking_listings"
  key_links:
    - from: "parking_listings"
      to: "geometry column"
      via: "PostGIS extension"
      pattern: "CREATE EXTENSION.*postgis"
    - from: "parking_listings"
      to: "location"
      via: "Point geometry"
      pattern: "location.*geometry(Point,4326)"

---

<objective>
Create database schema for parking discovery with PostGIS geospatial support using self-hosted Supabase.

Purpose: Enable fast location-based queries for nearby parking search with full local development environment.

Output: Docker Compose Supabase setup, raw SQL migrations, and Supabase JS client configuration.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-authentication-discovery-core/01-RESEARCH.md
@.planning/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup self-hosted Supabase via Docker Compose</name>
  <files>docker-compose.yml</files>
  <action>Create docker-compose.yml with official Supabase containers: PostgreSQL 15 + PostGIS, Auth, Realtime, Storage, API, Studio. Configure localhost ports, default secrets, and volume mounts. Add .env.local with Supabase URL and anon key pointed to localhost.</action>
  <verify>
    <automated>docker compose up -d && sleep 10 && curl http://localhost:54321</automated>
  </verify>
  <done>Supabase Studio accessible at http://localhost:54323</done>
</task>

<task type="auto">
  <name>Task 2: Write raw SQL migration</name>
  <files>supabase/migrations/01_initial.sql</files>
  <action>Create raw SQL migration: enable PostGIS extension, create users table with auth integration, create admin_users table, create parking_listings table with geometry(Point,4326) location column. Add GIST index on location. Write RLS policies inline.</action>
  <verify>
    <automated>psql -U postgres -h localhost -d postgres -f supabase/migrations/01_initial.sql</automated>
  </verify>
  <done>All tables, indexes and policies created successfully</done>
</task>

<task type="auto">
  <name>Task 3: Configure Supabase JS client</name>
  <files>src/lib/supabase.ts</files>
  <action>Create Supabase client factory pointing to localhost:54321 with anon key. Export type-safe database types generated via Supabase CLI.</action>
  <verify>
    <automated>npx tsc --noEmit src/lib/supabase.ts</automated>
  </verify>
  <done>Supabase client connects successfully to local instance</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Database | Authenticated queries with RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Database records | mitigate | RLS policies, input validation |
| T-01-08 | Information Disclosure | Parking data | mitigate | Status-based visibility (public only active) |
| T-01-09 | Denial of Service | Query performance | mitigate | GIST indexes on geometry |
</threat_model>

<verification>
Verify tables created, PostGIS enabled, geometry columns work, indexes applied, basic RLS policies functional.
</verification>

<success_criteria>
- Supabase running locally via Docker Compose
- parking_listings table with PostGIS geometry
- auth.users and admin_users tables
- Raw SQL migration applied successfully
- GIST index on location column
- Supabase JS client connects and runs queries
</success_criteria>

<output>
After completion, create .planning/phases/01-authentication-discovery-core/01-02-SUMMARY.md
</output>