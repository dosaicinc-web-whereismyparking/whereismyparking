---
phase: 01-authentication-discovery-core
plan: 02
type: execute
wave: 1
depends_on: ["01-00"]
files_modified: ["prisma/schema.prisma", "supabase/migrations/01_initial.sql"]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Parking listings stored with location geometry"
    - "Users table supports OTP authentication"
    - "Admin whitelist table exists"
    - "PostGIS enabled for geospatial queries"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Type-safe database schema"
      contains: "model ParkingListing"
    - path: "supabase/migrations/01_initial.sql"
      provides: "Database migration script"
      contains: "CREATE TABLE parking_listings"
  key_links:
    - from: "parking_listings"
      to: "geometry column"
      via: "PostGIS extension"
      pattern: "CREATE EXTENSION.*postgis"
    - from: "ParkingListing"
      to: "location"
      via: "Point geometry"
      pattern: "location.*geometry"

---

<objective>
Create database schema for parking discovery with PostGIS geospatial support.

Purpose: Enable fast location-based queries for nearby parking search.

Output: Prisma schema and Supabase migration for parking listings, users, and admin tables.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-authentication-discovery-core/01-RESEARCH.md
@.planning/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Prisma schema with PostGIS models</name>
  <files>prisma/schema.prisma</files>
  <action>Add PostGIS extension to schema.prisma. Create User model with id, phone, created_at. Create AdminUser model with user_id FK to User. Create ParkingListing model with id, name, address, location (geometry Point), type (Public/Private), coverage (Open/Covered/Multi), available_hours (JSON), status (Active/Pending), created_at. Add RLS policies placeholders.</action>
  <verify>
    <automated>npx prisma validate</automated>
  </verify>
  <done>Prisma schema validates with PostGIS geometry types</done>
</task>

<task type="auto">
  <name>Task 2: Generate and apply Supabase migration</name>
  <files>supabase/migrations/01_initial.sql</files>
  <action>Run npx prisma migrate dev --create-only to generate migration SQL. Move to supabase/migrations/01_initial.sql. Apply migration with supabase db push. Enable PostGIS extension. Create GIST index on parking_listings.location.</action>
  <verify>
    <automated>supabase db push && npx prisma db push</automated>
  </verify>
  <done>Database schema applied with PostGIS and indexes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Database | Authenticated queries with RLS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | Database records | mitigate | RLS policies, input validation |
| T-01-08 | Information Disclosure | Parking data | mitigate | Status-based visibility (public only active) |
| T-01-09 | Denial of Service | Query performance | mitigate | GIST indexes on geometry |
</threat_model>

<verification>
Verify tables created, PostGIS enabled, geometry columns work, indexes applied, basic RLS policies functional.
</verification>

<success_criteria>
- ParkingListing table with PostGIS geometry
- User and AdminUser tables
- Migration applied successfully
- GIST index on location column
- Prisma client generates correct types
</success_criteria>

<output>
After completion, create .planning/phases/01-authentication-discovery-core/01-02-SUMMARY.md
</output>