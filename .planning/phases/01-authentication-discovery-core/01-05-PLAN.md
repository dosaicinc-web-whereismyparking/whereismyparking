---
phase: 01-authentication-discovery-core
plan: 05
type: execute
wave: 3
depends_on: ["01-00", "01-03", "01-04"]
files_modified: ["src/components/Map.tsx", "src/components/ParkingList.tsx", "src/app/page.tsx"]
autonomous: true
requirements: ["DISC-01", "DISC-02", "DISC-03", "DISC-04", "DISC-05", "DISC-06", "LOC-05", "LOC-06"]
user_setup: []

must_haves:
  truths:
    - "Interactive map shows parking locations with pins"
    - "Parking list displays name, distance, type, coverage"
    - "Navigate button opens Google Maps with directions"
    - "Map updates dynamically on pan/zoom"
    - "UI works on mobile browsers"
    - "No API keys required for map rendering"
  artifacts:
    - path: "src/components/Map.tsx"
      provides: "MapLibre GL map component"
      exports: ["Map"]
    - path: "src/components/ParkingList.tsx"
      provides: "Parking list with filters"
      exports: ["ParkingList"]
    - path: "src/app/page.tsx"
      provides: "Main discovery page"
      contains: "<Map"
  key_links:
    - from: "useGeolocation"
      to: "/api/parking/nearby"
      via: "useEffect fetch"
      pattern: "fetch.*nearby"
    - from: "Map pins"
      to: "ParkingList items"
      via: "pin click -> highlight"
      pattern: "onClick.*highlight"
    - from: "Navigate button"
      to: "Google Maps"
      via: "deep-link URL"
      pattern: "maps\.google\.com.*directions"
    - from: "Map component"
      to: "OpenFreeMap tiles"
      via: "style URL"
      pattern: "tiles.openfreemap.org/styles/liberty"

---

<objective>
Create the main parking discovery interface with map and list views using open source MapLibre GL JS.

Purpose: Deliver the core user experience for finding nearby parking without third-party API key dependencies.

Output: Responsive web interface with location-based search and navigation using open map tiles.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-authentication-discovery-core/01-RESEARCH.md
@.planning/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Map component with MapLibre GL JS</name>
  <files>src/components/Map.tsx</files>
  <action>Create Map component using maplibre-gl npm package. Use OpenFreeMap liberty style: https://tiles.openfreemap.org/styles/liberty. No API key required. Accept parking data prop, render pins at coordinates with clustering. Handle map pan/zoom events to refetch nearby data. Add click handlers for pin selection. Use TypeScript interfaces for parking data. Style with Tailwind.</action>
  <verify>
    <automated>npx tsc --noEmit src/components/Map.tsx</automated>
  </verify>
  <done>Map displays pins and handles interactions without API keys</done>
</task>

<task type="auto">
  <name>Task 2: Create ParkingList component</name>
  <files>src/components/ParkingList.tsx</files>
  <action>Create ParkingList component accepting parking array and selectedId. Render list items with name, distance, type, coverage, available hours. Add filter dropdowns for type and coverage. Include Navigate button that opens Google Maps directions URL. Highlight selected item. Use Tailwind for responsive mobile design.</action>
  <verify>
    <automated>npm run test:unit -- --testPathPattern=discovery.test.ts --testNamePattern="Parking list"</automated>
  </verify>
  <done>Parking list renders with filters and navigation</done>
</task>

<task type="auto">
  <name>Task 3: Wire main discovery page</name>
  <files>src/app/page.tsx</files>
  <action>Update main page to use useGeolocation hook for location. Fetch nearby parking on location change. Display Map and ParkingList side-by-side on desktop, stacked on mobile. Handle loading states and errors. Add search fallback input. Ensure anonymous access (no auth required).</action>
  <verify>
    <automated>npx tsc --noEmit src/app/page.tsx</automated>
  </verify>
  <done>Main page integrates all components for discovery</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component → API | Client-side data fetching |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Information Disclosure | Parking locations | accept | Public data, no sensitive information |
</threat_model>

<verification>
Test full discovery flow: location access, search, map display, list interaction, navigation deep-link, mobile responsiveness.
</verification>

<success_criteria>
- Map shows parking pins with clustering
- List displays sorted results with filters
- Navigation opens Google Maps
- Pan/zoom refetches data
- Mobile-optimized interface
- Anonymous access works
- All remaining DISC requirements satisfied
</success_criteria>

<output>
After completion, create .planning/phases/01-authentication-discovery-core/01-05-SUMMARY.md
</output>