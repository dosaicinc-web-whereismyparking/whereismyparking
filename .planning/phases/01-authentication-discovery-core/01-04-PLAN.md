---
phase: 01-authentication-discovery-core
plan: 04
type: execute
wave: 2
depends_on: ["01-00", "01-02"]
files_modified: ["src/app/api/parking/nearby/route.ts"]
autonomous: true
requirements: ["DISC-02", "DISC-03", "DISC-04", "DISC-06"]
user_setup: []

must_haves:
  truths:
    - "Nearby parking search returns results in <500ms"
    - "Results filtered by type (Public/Private) and coverage"
    - "Availability timing information included"
    - "Placeholder response when no parking found"
    - "API supports dynamic queries for map pan/zoom"
  artifacts:
    - path: "src/app/api/parking/nearby/route.ts"
      provides: "Geospatial parking search endpoint"
      exports: ["GET"]
  key_links:
    - from: "GET /api/parking/nearby"
      to: "parking_listings"
      via: "ST_DWithin(geometry, point, 2000)"
      pattern: "ST_DWithin"
    - from: "query params"
      to: "SQL WHERE"
      via: "type and coverage filters"
      pattern: "WHERE.*status.*Active"
    - from: "results"
      to: "JSON response"
      via: "Prisma select"
      pattern: "select.*name.*distance"

---

<objective>
Create high-performance API for location-based parking search with filtering.

Purpose: Enable fast discovery of nearby parking with user-defined criteria.

Output: RESTful API endpoint returning paginated parking results with distances.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-authentication-discovery-core/01-RESEARCH.md
@.planning/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement nearby parking search API</name>
  <files>src/app/api/parking/nearby/route.ts</files>
  <action>Create GET route accepting query params: lat, lng, radius (default 2km), type, coverage, limit (default 50). Use Prisma with PostGIS ST_DWithin for spatial query, filter by status=Active, type, coverage. Calculate distance using ST_Distance, sort by distance ASC. Include available_hours in response. Handle no results with placeholder message. Use zod for query validation.</action>
  <verify>
    <automated>npm run test:unit -- --testPathPattern=discovery.test.ts --testNamePattern="Nearby search API"</automated>
  </verify>
  <done>Nearby search API returns filtered results with distances</done>
</task>

<task type="auto">
  <name>Task 2: Add performance optimizations and error handling</name>
  <files>src/app/api/parking/nearby/route.ts</files>
  <action>Add response caching for 5 minutes on identical queries. Implement pagination with cursor-based next page. Add error handling for invalid coordinates, database errors. Ensure response time <500ms with proper indexing. Add CORS headers for map integration.</action>
  <verify>
    <automated>npm run test:unit -- --testPathPattern=discovery.test.ts --testNamePattern="API performance and errors"</automated>
  </verify>
  <done>API optimized for <500ms response with caching and pagination</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | User-controlled search parameters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Tampering | Search parameters | mitigate | Zod validation on lat/lng/radius |
| T-01-12 | Denial of Service | Expensive queries | mitigate | Radius limit (max 5km), rate limiting |
</threat_model>

<verification>
Test search with various filters, performance benchmarking, error responses, pagination, caching behavior.
</verification>

<success_criteria>
- Search returns nearby parking in <500ms
- Filters work correctly
- Availability timing included
- No results show placeholder
- API ready for map integration
- All DISC and remaining LOC requirements satisfied
</success_criteria>

<output>
After completion, create .planning/phases/01-authentication-discovery-core/01-04-SUMMARY.md
</output>