---
phase: 01-authentication-discovery-core
plan: 03
type: execute
wave: 2
depends_on: ["01-00", "01-02"]
files_modified: ["src/hooks/useGeolocation.ts", "src/utils/distance.ts"]
autonomous: true
requirements: ["LOC-01", "LOC-02", "LOC-03", "LOC-04"]
user_setup: []

must_haves:
  truths:
    - "Browser GPS location obtained with permission"
    - "Manual city/area search fallback when GPS denied"
    - "Distance calculated from user to each parking listing"
    - "Parking listings sorted by distance (nearest first)"
  artifacts:
    - path: "src/hooks/useGeolocation.ts"
      provides: "React hook for location access"
      exports: ["useGeolocation"]
    - path: "src/utils/distance.ts"
      provides: "Distance calculation functions"
      exports: ["calculateDistance", "sortByDistance"]
  key_links:
    - from: "useGeolocation"
      to: "navigator.geolocation"
      via: "getCurrentPosition"
      pattern: "navigator\.geolocation\.getCurrentPosition"
    - from: "calculateDistance"
      to: "haversine formula"
      via: "lat/lng coordinates"
      pattern: "Math\.acos.*Math\.sin"
    - from: "parking list"
      to: "sorted list"
      via: "sortByDistance"
      pattern: "sort.*distance"

---

<objective>
Implement user location services with GPS access and distance calculations.

Purpose: Enable location-aware parking discovery with accurate positioning.

Output: Geolocation hook and distance utilities for sorting nearby results.
</objective>

<execution_context>
@$HOME/.config/kilo/get-shit-done/workflows/execute-plan.md
@$HOME/.config/kilo/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-authentication-discovery-core/01-RESEARCH.md
@.planning/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create geolocation React hook</name>
  <files>src/hooks/useGeolocation.ts</files>
  <action>Create useGeolocation hook using navigator.geolocation.getCurrentPosition with enableHighAccuracy and 10s timeout. Handle permission states (granted/denied/prompt). Return {position, error, loading, requestLocation}. On denied, show fallback UI for manual city input. Use TypeScript for position interface.</action>
  <verify>
    <automated>npm run test:unit -- --testPathPattern=location.test.ts --testNamePattern="Geolocation hook"</automated>
  </verify>
  <done>Geolocation hook handles permissions and provides position or fallback</done>
</task>

<task type="auto">
  <name>Task 2: Implement distance calculation utilities</name>
  <files>src/utils/distance.ts</files>
  <action>Create calculateDistance function using haversine formula for km distance between two lat/lng points. Create sortByDistance function that sorts parking array by distance from user location. Handle null positions gracefully. Export both functions with TypeScript types.</action>
  <verify>
    <automated>npx tsx -e "import { calculateDistance } from './src/utils/distance'; console.log(calculateDistance({lat:0,lng:0}, {lat:1,lng:1}))"</automated>
  </verify>
  <done>Distance calculations accurate and sorting works</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Hook | User-controlled location data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Information Disclosure | User location | accept | Location required for core feature, privacy notice shown |
</threat_model>

<verification>
Test geolocation permission flow, fallback to manual search, distance calculations accuracy, sorting by distance.
</verification>

<success_criteria>
- GPS location obtained when permitted
- Manual search fallback functional
- Distances calculated correctly
- Listings sorted nearest first
- All LOC requirements satisfied
</success_criteria>

<output>
After completion, create .planning/phases/01-authentication-discovery-core/01-03-SUMMARY.md
</output>