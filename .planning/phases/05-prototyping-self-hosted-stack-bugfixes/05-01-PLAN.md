---
phase: 05-prototyping-self-hosted-stack-bugfixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/ListingForm.tsx, src/components/Map.tsx, package.json, tests/discovery.test.tsx]
autonomous: true
requirements: [PH5-BUG-01, PH5-MAP-01]
user_setup: []

must_haves:
  truths:
    - "ListingForm Step 1 validation correctly blocks progression when fields are invalid"
    - "Map component renders using MapLibre instead of Mapbox"
    - "Map component does not require a Mapbox token to function"
  artifacts:
    - path: "src/components/Map.tsx"
      provides: "MapLibre GL JS integration"
    - path: "src/components/ListingForm.tsx"
      provides: "Fixed validation logic"
  key_links:
    - from: "src/components/Map.tsx"
      to: "maplibre-gl"
      via: "import from react-map-gl/maplibre"
---

<objective>
Fix critical validation bypass in ListingForm.tsx and migrate the map component from Mapbox to MapLibre GL JS.

Purpose: 
1. Ensure data integrity by preventing users from bypassing form validation.
2. Reduce external dependencies and costs by moving to an open-source map engine.
Output:
- Fixed validation in ListingForm.tsx
- Map component updated to use MapLibre GL JS
- Updated package.json with MapLibre dependencies
- Updated discovery.test.tsx with ListingForm validation tests
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-prototyping-self-hosted-stack-bugfixes/05-CONTEXT.md
@src/components/ListingForm.tsx
@src/components/Map.tsx
@tests/discovery.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix validation bypass in ListingForm.tsx</name>
  <files>src/components/ListingForm.tsx, tests/discovery.test.tsx</files>
  <action>
    - Modify `handleStep1Next` in `src/components/ListingForm.tsx` to properly await and check the result of `trigger(['name', 'address'])`.
    - Explicitly block transition: `const isStepValid = await trigger(['name', 'address']); if (!isStepValid) return; setStep(2);`.
    - Enhance error feedback: Ensure `trigger` populates the `errors` object and that validation failures are visible to the user.
    - Update `tests/discovery.test.tsx` to include a new describe block for `ListingForm` that verifies:
      - Step 1 cannot progress if name or address are empty.
      - Error messages appear when validation fails.
      - Progression to Step 2 only occurs when fields are valid.
  </action>
  <verify>
    <automated>npm test tests/discovery.test.tsx</automated>
  </verify>
  <done>ListingForm Step 1 transition is blocked on validation failure, verified by tests.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Map component to MapLibre GL JS</name>
  <files>src/components/Map.tsx, package.json</files>
  <action>
    - Install `maplibre-gl` and remove `mapbox-gl` and `@types/mapbox-gl`.
    - Update `src/components/Map.tsx`:
      - Change import from `react-map-gl/mapbox` to `react-map-gl/maplibre`.
      - Remove `mapboxAccessToken` prop from `<Map>`.
      - Remove `MAPBOX_TOKEN` constant and its usage.
      - Update `mapStyle` to use a MapLibre compatible style URL (e.g., "https://demotiles.maplibre.org/style.json" for initial testing).
      - Ensure all `LayerProps` and `Source` components are compatible with MapLibre.
      - Update `clusterCountLayer` font if necessary (MapLibre might not support "DIN Offc Pro Medium"). Use "Open Sans Regular" or similar generic font.
    - Update CSS import to `maplibre-gl/dist/maplibre-gl.css`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Map component renders using MapLibre without Mapbox token.</done>
</task>

<task type="auto">
  <name>Task 3: Update Environment Variables for Map Provider</name>
  <files>.env.example, .env.local</files>
  <action>
    - Add `NEXT_PUBLIC_MAP_PROVIDER=maplibre` to `.env.example`.
    - Add `NEXT_PUBLIC_MAPLIBRE_STYLE_URL=https://demotiles.maplibre.org/style.json` to `.env.example`.
    - Update `.env.local` accordingly.
    - Update `src/components/Map.tsx` to use `process.env.NEXT_PUBLIC_MAPLIBRE_STYLE_URL` as the default `mapStyle`.
  </action>
  <verify>
    <automated>grep NEXT_PUBLIC_MAP_PROVIDER .env.example</automated>
  </verify>
  <done>Environment variables for map provider are defined.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Form → Validation | Untrusted user input must be validated before proceeding to next step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | ListingForm | mitigate | Ensure `trigger()` result is checked and explicitly blocks step transition on validation failure. |
| T-05-01-02 | Information Disclosure | Map Component | mitigate | Remove Mapbox API token usage to prevent leak of paid credentials. |
</threat_model>

<verification>
- Run `npm run dev` and verify ListingForm Step 1 validation manually.
- Verify Map component renders on discovery page without Mapbox errors.
- Run `npm test tests/discovery.test.tsx` to verify automated checks.
</verification>

<success_criteria>
- ListingForm validation blocks empty submissions.
- Map component uses MapLibre GL JS.
- No Mapbox dependencies or tokens remain in active code.
</success_criteria>

<output>
After completion, create `.planning/phases/05-prototyping-self-hosted-stack-bugfixes/05-01-SUMMARY.md`
</output>
