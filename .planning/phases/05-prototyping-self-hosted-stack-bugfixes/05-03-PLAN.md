---
phase: 05-prototyping-self-hosted-stack-bugfixes
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: [src/lib/supabase.ts, src/lib/admin-auth.ts, src/lib/prisma.ts, package.json, prisma/]
autonomous: true
requirements: [PH5-INFRA-03, PH5-CLEAN-01]
user_setup: []

must_haves:
  truths:
    - "Supabase client can point to both cloud and local stacks via env vars"
    - "Admin authentication and logging no longer use Prisma"
    - "Prisma dependencies and files are completely removed from the project"
  artifacts:
    - path: "src/lib/supabase.ts"
      provides: "Switchable Supabase client"
    - path: "src/lib/admin-auth.ts"
      provides: "Prisma-free admin logic"
  key_links:
    - from: "src/lib/admin-auth.ts"
      to: "supabase client"
      via: "replacement of prisma calls"
---

<objective>
Finalize the switch to the self-hosted stack and remove Prisma entirely from the codebase.

Purpose:
1. Complete the architectural shift to a unified Supabase-only stack.
2. Reduce project footprint and complexity by removing the Prisma ORM.
Output:
- Configurable Supabase client supporting multiple stacks
- Prisma-free application logic with pluralized table names
- Cleaned up package.json and project structure
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-prototyping-self-hosted-stack-bugfixes/05-CONTEXT.md
@src/lib/supabase.ts
@src/lib/admin-auth.ts
@src/lib/prisma.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Supabase client for stack switching</name>
  <files>src/lib/supabase.ts</files>
  <action>
    - Update `src/lib/supabase.ts` to use environment variables for `supabaseUrl` and `supabaseAnonKey`.
    - Support runtime switching if needed, or simply rely on `NEXT_PUBLIC_SUPABASE_URL` being changed in `.env.local`.
    - Ensure `supabaseUrl` points to `http://localhost:8000` (or the Mac Mini IP) when local.
    - Add `SUPABASE_SERVICE_ROLE_KEY` to server-side usage if needed for bypass logic.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Supabase client is configurable via environment variables.</done>
</task>

<task type="auto">
  <name>Task 2: Convert Admin Auth to Supabase client</name>
  <files>src/lib/admin-auth.ts</files>
  <action>
    - Remove `import { prisma } from '@/lib/prisma'` from `src/lib/admin-auth.ts`.
    - Update `requireAdminSession`:
      - Replace `prisma.adminUser.findUnique` with `supabase.from('admin_users').select().eq('user_id', data.user.id).single()`. Note the plural `admin_users` per Prisma `@map`.
    - Update `logAdminActivity`:
      - Replace `prisma.adminActivity.create` with `supabase.from('admin_activities').insert({...})`. Note the plural `admin_activities` per Prisma `@map`.
    - Ensure all field names match the SQL schema generated in Plan 02 (e.g., `user_id` instead of `userId`).
  </action>
  <verify>
    <automated>npm test tests/admin-routes.test.ts</automated>
  </verify>
  <done>Admin logic is fully migrated to Supabase client using correct plural table names.</done>
</task>

<task type="auto">
  <name>Task 3: Remove Prisma from the project</name>
  <files>package.json, prisma/, src/lib/prisma.ts</files>
  <action>
    - Run `npm uninstall prisma @prisma/client`.
    - Delete `prisma/` directory.
    - Delete `src/lib/prisma.ts`.
    - Perform a project-wide search for "prisma" and remove any remaining imports or calls.
    - Update `package.json` scripts if they referenced Prisma (e.g., `prisma generate`).
  </action>
  <verify>
    <automated>! grep -r "prisma" src/ --exclude-dir=node_modules</automated>
  </verify>
  <done>Prisma is completely removed from the project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API → Database | Service role key usage must be restricted to server-side only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Spoofing | Admin Auth | mitigate | Ensure RLS policies in Supabase are correctly enforced even after removing Prisma's middle layer. |
| T-05-03-02 | Information Disclosure | Env Vars | mitigate | Do not expose `SUPABASE_SERVICE_ROLE_KEY` to the client-side. |
</threat_model>

<verification>
- Verify build succeeds without Prisma.
- Verify existing admin tests pass (or are updated to work with Supabase mock if needed).
</verification>

<success_criteria>
- Project no longer has any Prisma dependencies.
- Application works against the self-hosted stack configuration.
- Clean and unified Supabase usage throughout the codebase.
</success_criteria>

<output>
After completion, create `.planning/phases/05-prototyping-self-hosted-stack-bugfixes/05-03-SUMMARY.md`
</output>
